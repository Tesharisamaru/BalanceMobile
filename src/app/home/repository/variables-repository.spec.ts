import { VariablesRepository } from './variables-repository';

describe('VariablesRepository', () => {
  it('should create an instance', () => {
    expect(new VariablesRepository()).toBeTruthy();
  });
});
